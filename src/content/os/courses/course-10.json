{
  "meta": {
    "id": "os-c10",
    "title": { "en": "Course 10: Process Notification via Signals", "ro": "Cursul 10: Notificarea proceselor prin semnale" },
    "shortTitle": { "en": "C10: Signals", "ro": "C10: Semnale" },
    "source": { "en": "Prof. C. Vidrascu — Lecture 10 slides", "ro": "Prof. C. Vidrascu — Curs 10" }
  },
  "steps": [
    {
      "id": "os-c10-intro",
      "title": { "en": "Introduction to Signals", "ro": "Introducere in semnale" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "Signals used by UNIX-family operating systems represent a fundamental mechanism for process manipulation and inter-process communication, ensuring the handling of asynchronous events in the system. A **signal** is a software interrupt generated when a certain event occurs and transmitted by the operating system to a specific process.",
            "ro": "Semnalele folosite de sistemele de operare din familia UNIX reprezinta un mecanism fundamental de manipulare a proceselor si de comunicare intre procese, ce asigura tratarea evenimentelor asincrone aparute in sistem. Un **semnal** este o intrerupere software generata in momentul producerii unui anumit eveniment si transmisa de sistemul de operare unui anumit proces."
          }
        },
        {
          "type": "think",
          "question": {
            "en": "Why are signals sometimes called 'software interrupts'? How do they differ from hardware interrupts?",
            "ro": "De ce semnalele sunt numite uneori 'intreruperi software'? Cum difera ele de intreruperile hardware?"
          },
          "answer": {
            "en": "Signals are called software interrupts because, like hardware interrupts, they cause the current execution flow to be suspended so that a handler function can execute. The difference is that hardware interrupts are generated by physical devices (keyboard, disk, network card) and handled by the kernel, while signals are generated by the OS kernel or by processes and delivered to user-space processes. Both share the concept of asynchronous notification and handler execution.",
            "ro": "Semnalele sunt numite intreruperi software deoarece, la fel ca intreruperile hardware, ele cauzeaza suspendarea fluxului curent de executie pentru ca o functie handler sa se execute. Diferenta este ca intreruperile hardware sunt generate de dispozitive fizice (tastatura, disc, placa de retea) si tratate de kernel, in timp ce semnalele sunt generate de kernelul SO sau de procese si livrate proceselor din spatiul utilizator. Ambele impartasesc conceptul de notificare asincrona si executie de handler."
          }
        }
      ]
    },
    {
      "id": "os-c10-general-notions",
      "title": { "en": "General Notions About Signals", "ro": "Notiuni generale despre semnale" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "A signal is **generated** by the occurrence of an exceptional event (which can be an error, an external event, or an explicit request). Every signal has an associated **type**, represented by a positive integer (encoding its cause), and a **destination process**. Once generated, the signal is placed in the system's **signal queue**, from where it is extracted and transmitted to the destination process by the operating system.",
            "ro": "Un semnal este **generat** de aparitia unui eveniment exceptional (care poate fi o eroare, un eveniment extern sau o cerere explicita). Orice semnal are asociat un **tip**, reprezentat printr-un numar intreg pozitiv (ce codifica cauza sa), si un **proces destinatar**. Odata generat, semnalul este pus in **coada de semnale** a sistemului, de unde este extras si transmis procesului destinatar de catre sistemul de operare."
          }
        },
        {
          "type": "learn",
          "content": {
            "en": "Signal transmission to the destination is done immediately after the signal enters the signal queue, with one exception: if reception of that signal type has been **blocked** by the destination process, then transmission will occur only when the destination process unblocks reception of that signal type.",
            "ro": "Transmiterea semnalului catre destinatar se face imediat dupa ce semnalul a ajuns in coada de semnale, cu o exceptie: daca primirea semnalelor de tipul respectiv a fost **blocata** de catre procesul destinatar, atunci transmiterea semnalului se va face abia in momentul cand procesul destinatar va debloca primirea acelui tip de semnal."
          }
        },
        {
          "type": "definition",
          "term": { "en": "Signal Handler", "ro": "Handler de semnal" },
          "content": {
            "en": "When the destination process receives a signal, it **interrupts its execution** and executes a specific action (a signal handling function called a **signal handler**) attached to that signal type. After the handler completes, the process resumes execution from the point where it was interrupted (with some exceptions -- certain signals will cause forced termination of the process).",
            "ro": "Cand procesul destinatar primeste un semnal, isi **intrerupe executia** si executa o anumita actiune (o functie de tratare a semnalului numita **handler de semnal**) atasata tipului de semnal primit. Dupa aceea, procesul isi va relua executia din punctul in care a fost intrerupt (cu anumite exceptii -- unele semnale vor cauza terminarea fortata a procesului)."
          }
        },
        {
          "type": "callout",
          "variant": "info",
          "content": {
            "en": "In conclusion, each signal type has an associated action (a **handler**) specific to that signal type. The process lifecycle of a signal is: **generation** (event occurs) -> **queued** (placed in signal queue) -> **delivery** (transmitted to destination process) -> **handling** (handler executes).",
            "ro": "In concluzie, fiecare tip de semnal are asociat o actiune (un **handler**) specifica acelui tip de semnal. Ciclul de viata al unui semnal este: **generare** (evenimentul apare) -> **in coada** (pus in coada de semnale) -> **livrare** (transmis procesului destinatar) -> **tratare** (handler-ul se executa)."
          }
        }
      ]
    },
    {
      "id": "os-c10-signal-categories",
      "title": { "en": "Signal Categories", "ro": "Categorii de semnale" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "The events that generate signals fall into three categories: **errors** (in the destination process), **external events** (of the destination process), and **explicit requests**. An **error** means the program performed an invalid operation and cannot continue execution. Not all errors generate signals -- only those that can occur at any point in the program, such as: division by zero, accessing an invalid memory address, etc.",
            "ro": "Evenimentele ce genereaza semnale se impart in trei categorii: **erori** (in procesul destinatar), **evenimente externe** (procesului destinatar) si **cereri explicite**. O **eroare** inseamna ca programul a facut o operatie invalida si nu poate sa-si continue executia. Nu toate erorile genereaza semnale, ci doar acele erori care pot apare in orice punct al programului, cum ar fi: impartirea la zero, accesarea unei adrese de memorie invalide, etc."
          }
        },
        {
          "type": "learn",
          "content": {
            "en": "**External events** are generally related to I/O operations and actions of other processes, such as: data arrival on a socket or pipe, child process termination, timer interval expiration, program suspension or termination by the user (e.g., pressing `Ctrl+C`). An **explicit request** means the generation of a signal by another (or the same) process, through the `kill` or `sigqueue` primitives.",
            "ro": "**Evenimentele externe** sunt in general legate de operatiile I/O sau de actiunile altor procese, cum ar fi: sosirea datelor pe un socket sau pipe, terminarea unui proces fiu, expirarea intervalului de timp setat pentru o alarma, suspendarea ori terminarea programului de catre utilizator (de ex., prin apasarea tastelor `Ctrl+C`). O **cerere explicita** inseamna generarea unui semnal de catre un alt (sau acelasi) proces, prin apelul uneia dintre primitivele `kill` sau `sigqueue`."
          }
        },
        {
          "type": "definition",
          "term": { "en": "Synchronous Signal", "ro": "Semnal sincron" },
          "content": {
            "en": "A signal generated by a specific action in the program, delivered (if not blocked) to the respective process during the execution of that action. Events that generate synchronous signals: errors and explicit requests of a process to generate signals for itself.",
            "ro": "Un semnal generat de o anumita actiune specifica in program, livrat (daca nu este blocat) procesului respectiv in timpul executiei acelei actiuni. Evenimente ce genereaza semnale sincrone: erorile si cererile explicite ale unui proces de a genera semnale pentru el insusi."
          }
        },
        {
          "type": "definition",
          "term": { "en": "Asynchronous Signal", "ro": "Semnal asincron" },
          "content": {
            "en": "A signal generated by an event outside the control zone of the receiving process; a signal received during the execution of the destination process, at an unpredictable moment. Events that generate asynchronous signals: external events and explicit requests of a process to generate signals destined for other processes.",
            "ro": "Un semnal generat de un eveniment din afara zonei de control a procesului care il receptioneaza; un semnal ce este receptionat in timpul executiei procesului destinatar, la un moment de timp ce nu poate fi anticipat. Evenimente ce genereaza semnale asincrone: evenimentele externe si cererile explicite ale unui proces de a genera semnale destinate altor procese."
          }
        },
        {
          "type": "quiz",
          "questions": [
            {
              "question": { "en": "Which of the following is an example of a synchronous signal?", "ro": "Care dintre urmatoarele este un exemplu de semnal sincron?" },
              "options": [
                { "text": { "en": "A child process termination signal (SIGCHLD)", "ro": "Un semnal de terminare a procesului fiu (SIGCHLD)" }, "correct": false },
                { "text": { "en": "A division by zero error (SIGFPE)", "ro": "O eroare de impartire la zero (SIGFPE)" }, "correct": true },
                { "text": { "en": "A user pressing Ctrl+C (SIGINT)", "ro": "Utilizatorul apasa Ctrl+C (SIGINT)" }, "correct": false },
                { "text": { "en": "A signal sent by another process via kill()", "ro": "Un semnal trimis de alt proces prin kill()" }, "correct": false }
              ],
              "explanation": { "en": "Division by zero (SIGFPE) is a synchronous signal because it is generated by a specific action within the program itself. The other options are all asynchronous signals -- they come from external events or other processes.", "ro": "Impartirea la zero (SIGFPE) este un semnal sincron deoarece este generat de o actiune specifica din programul insusi. Celelalte optiuni sunt semnale asincrone -- provin din evenimente externe sau de la alte procese." }
            }
          ]
        }
      ]
    },
    {
      "id": "os-c10-kill-primitive",
      "title": { "en": "The kill Primitive for Signal Generation", "ro": "Primitiva kill pentru generarea unui semnal" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "A process can explicitly request the generation of a signal through the `kill` system call. The effect of calling `kill` is that a signal of the specified type is generated, destined for the specified process.",
            "ro": "Un proces poate cere explicit generarea unui semnal prin apelul de sistem `kill`. Efectul executiei functiei `kill` este ca se genereaza un semnal de tipul specificat, destinat procesului specificat."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "// Interface of the kill function ([5]):\n#include <signal.h>\n\nint kill(pid_t pid, int sig);\n// pid = PID of the destination process\n// sig = signal type\n// Returns: 0 on success, -1 on error"
        },
        {
          "type": "callout",
          "variant": "tip",
          "content": {
            "en": "Calling `kill(pid, 0)` does not generate any signal, but it is useful for verifying the validity of a PID (i.e., whether a process with that PID exists at the time of the call): it returns 0 if the specified PID is valid, or -1 otherwise.",
            "ro": "Apelul `kill(pid, 0)` nu genereaza niciun semnal, dar este util pentru verificarea validitatii PID-ului respectiv (i.e., daca exista un proces cu acel PID in momentul apelului): se returneaza 0 daca PID-ul specificat este valid, sau -1 in caz contrar."
          }
        },
        {
          "type": "learn",
          "content": {
            "en": "A process can also send signals to itself using the `raise` function from the C Standard Library. The call `raise(sig)` is equivalent to `kill(getpid(), sig)`.",
            "ro": "Un proces isi poate trimite semnale sie insusi folosind functia `raise` din biblioteca C Standard Library. Apelul `raise(sig)` este echivalent cu apelul `kill(getpid(), sig)`."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "#include <signal.h>\n\nint raise(int sig);\n// Effect: equivalent to kill(getpid(), sig)"
        },
        {
          "type": "code",
          "language": "bash",
          "code": "# Sending signals from the command line:\nUNIX> kill -sig pid\nUNIX> killall -sig process_name"
        },
        {
          "type": "quiz",
          "questions": [
            {
              "question": { "en": "What happens when you call kill(pid, 0)?", "ro": "Ce se intampla cand apelati kill(pid, 0)?" },
              "options": [
                { "text": { "en": "The process with the given PID is terminated", "ro": "Procesul cu PID-ul dat este terminat" }, "correct": false },
                { "text": { "en": "Signal 0 (SIGNULL) is sent to the process", "ro": "Semnalul 0 (SIGNULL) este trimis procesului" }, "correct": false },
                { "text": { "en": "No signal is sent; it only checks if the PID is valid", "ro": "Nu se trimite niciun semnal; doar se verifica daca PID-ul este valid" }, "correct": true },
                { "text": { "en": "An error is returned because signal 0 does not exist", "ro": "Se returneaza o eroare deoarece semnalul 0 nu exista" }, "correct": false }
              ],
              "explanation": { "en": "`kill(pid, 0)` is a special case: no signal is generated, but the call checks whether the specified PID is valid. It returns 0 if a process with that PID exists, or -1 otherwise. This is a common technique for checking process existence.", "ro": "`kill(pid, 0)` este un caz special: nu se genereaza niciun semnal, dar apelul verifica daca PID-ul specificat este valid. Returneaza 0 daca exista un proces cu acel PID, sau -1 in caz contrar. Aceasta este o tehnica comuna pentru verificarea existentei unui proces." }
            }
          ]
        }
      ]
    },
    {
      "id": "os-c10-posix-signal-types",
      "title": { "en": "Predefined POSIX Signal Types", "ro": "Tipurile predefinite de semnale din standardul POSIX" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "The predefined signal types in the POSIX standard can be classified as follows: **process termination signals** (`SIGHUP`, `SIGINT`, `SIGQUIT`, `SIGABRT`, `SIGTERM`, `SIGKILL`), **process control signals** (`SIGCHLD`, `SIGCONT`, `SIGSTOP`, `SIGTSTP`, `SIGTTIN`, `SIGTTOU`, `SIGTRAP`), **standard error signals** (`SIGFPE`, `SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGSYS`), **asynchronous I/O signals** (`SIGIO`, `SIGURG`), **alarm signals** (`SIGALRM`, `SIGVTALRM`, `SIGPROF`), and **other signal types** (`SIGUSR1`, `SIGUSR2`, `SIGPIPE`, `SIGXCPU`, `SIGXFSZ`). All the types listed above are called **standard signals**.",
            "ro": "Tipurile predefinite de semnale din standardul POSIX pot fi clasificate astfel: **semnale de terminare a proceselor** (`SIGHUP`, `SIGINT`, `SIGQUIT`, `SIGABRT`, `SIGTERM`, `SIGKILL`), **semnale pentru controlul proceselor** (`SIGCHLD`, `SIGCONT`, `SIGSTOP`, `SIGTSTP`, `SIGTTIN`, `SIGTTOU`, `SIGTRAP`), **semnale standard de eroare** (`SIGFPE`, `SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGSYS`), **semnale asincrone I/O** (`SIGIO`, `SIGURG`), **semnale de alarma** (`SIGALRM`, `SIGVTALRM`, `SIGPROF`), si **alte tipuri de semnale** (`SIGUSR1`, `SIGUSR2`, `SIGPIPE`, `SIGXCPU`, `SIGXFSZ`). Toate tipurile enumerate mai sus se numesc **semnale standard**."
          }
        },
        {
          "type": "learn",
          "content": {
            "en": "There is also a category of **real-time signals**, formed from the following signal types: `SIGRTMIN`, `SIGRTMIN+1`, ..., `SIGRTMIN+n`, ..., `SIGRTMAX`. Unlike standard signals, real-time signals do not have predefined semantics: the entire set of real-time signals can be used by applications for their own purposes. They also have different semantics -- for more details, consult the manual page `man 7 signal`.",
            "ro": "Mai exista si categoria **semnale real-time**, formata din urmatoarele tipuri de semnale: `SIGRTMIN`, `SIGRTMIN+1`, ..., `SIGRTMIN+n`, ..., `SIGRTMAX`. Spre deosebire de semnalele standard, semnalele in timp real nu au semantici predefinite: intregul set de semnale in timp real poate fi utilizat in scopuri definite de aplicatie. De asemenea, au semantici diferite -- pentru mai multe detalii, consultati pagina de manual `man 7 signal`."
          }
        },
        {
          "type": "code",
          "language": "bash",
          "code": "# List all predefined signal types with their numbers:\nUNIX> kill -l\n\n# View the manual page describing signal types:\nUNIX> man 7 signal"
        },
        {
          "type": "callout",
          "variant": "warning",
          "content": {
            "en": "Some standard signal types depend on the **hardware** of the computer being used. Because of this, there may be small differences in how signals are implemented across different computer architectures (some signals may not be implemented at all, or may be implemented differently). Example: `SIGBUS` (bus error) is not implemented in the Linux kernel for the i386 architecture. Always consult the documentation for your specific platform.",
            "ro": "O parte dintre tipurile de semnale standard depind de **hardware-ul** calculatorului utilizat. Din acest motiv, exista mici deosebiri in modul de implementare a acestor semnale pe diferite tipuri de arhitecturi de calculatoare (unele semnale sa nu fie implementate deloc, sau sa fie implementate cu mici diferente). Exemplu: `SIGBUS` (eroare de bus) nu este implementat in nucleul Linux pentru arhitectura hardware i386. Trebuie studiata documentatia tipului de calculator pe care il utilizati."
          }
        },
        {
          "type": "table",
          "headers": [
            { "en": "Category", "ro": "Categorie" },
            { "en": "Signals", "ro": "Semnale" },
            { "en": "Description", "ro": "Descriere" }
          ],
          "rows": [
            [
              { "en": "Termination", "ro": "Terminare" },
              { "en": "`SIGHUP`, `SIGINT`, `SIGQUIT`, `SIGABRT`, `SIGTERM`, `SIGKILL`", "ro": "`SIGHUP`, `SIGINT`, `SIGQUIT`, `SIGABRT`, `SIGTERM`, `SIGKILL`" },
              { "en": "Request process termination", "ro": "Solicita terminarea procesului" }
            ],
            [
              { "en": "Control", "ro": "Control" },
              { "en": "`SIGCHLD`, `SIGCONT`, `SIGSTOP`, `SIGTSTP`, `SIGTTIN`, `SIGTTOU`, `SIGTRAP`", "ro": "`SIGCHLD`, `SIGCONT`, `SIGSTOP`, `SIGTSTP`, `SIGTTIN`, `SIGTTOU`, `SIGTRAP`" },
              { "en": "Control process execution", "ro": "Controleaza executia procesului" }
            ],
            [
              { "en": "Error", "ro": "Eroare" },
              { "en": "`SIGFPE`, `SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGSYS`", "ro": "`SIGFPE`, `SIGSEGV`, `SIGBUS`, `SIGILL`, `SIGSYS`" },
              { "en": "Hardware/software errors", "ro": "Erori hardware/software" }
            ],
            [
              { "en": "Async I/O", "ro": "I/O asincron" },
              { "en": "`SIGIO`, `SIGURG`", "ro": "`SIGIO`, `SIGURG`" },
              { "en": "Asynchronous I/O events", "ro": "Evenimente I/O asincrone" }
            ],
            [
              { "en": "Alarm", "ro": "Alarma" },
              { "en": "`SIGALRM`, `SIGVTALRM`, `SIGPROF`", "ro": "`SIGALRM`, `SIGVTALRM`, `SIGPROF`" },
              { "en": "Timer expiration", "ro": "Expirarea timer-ului" }
            ],
            [
              { "en": "Other", "ro": "Altele" },
              { "en": "`SIGUSR1`, `SIGUSR2`, `SIGPIPE`, `SIGXCPU`, `SIGXFSZ`", "ro": "`SIGUSR1`, `SIGUSR2`, `SIGPIPE`, `SIGXCPU`, `SIGXFSZ`" },
              { "en": "User-defined and resource signals", "ro": "Semnale definite de utilizator si de resurse" }
            ]
          ]
        }
      ]
    },
    {
      "id": "os-c10-signal-handling",
      "title": { "en": "Signal Handling (Treatment)", "ro": "Tratarea semnalelor" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "For each signal type there is a default handling action, defined in the kernel of every UNIX operating system. This action is called the **default signal handler** attached to that signal type. When a signal is delivered to a process, execution is interrupted and there are three possibilities for the process to handle the signal: it executes the **default handler** associated with that signal type, it **ignores** the signal, or it executes a user-defined **handler** function (defined by the programmer who writes the program).",
            "ro": "Pentru fiecare tip de semnal exista o actiune implicita de tratare, definita in nucleul fiecarui sistem de operare UNIX. Aceasta actiune este denumita **handler-ul implicit de semnal** atasat acelui tip de semnal. Atunci cand semnalul este livrat procesului, executia acestuia este intrerupta si exista trei posibilitati de tratare a semnalului de catre procesul respectiv: fie se executa **handler-ul implicit** asociat tipului acelui semnal, fie se **ignora** semnalul, fie se executa o anumita functie **handler** utilizator (definita de programatorul care a scris programul respectiv)."
          }
        },
        {
          "type": "definition",
          "term": { "en": "Signal Corruption (Overriding)", "ro": "Coruperea unui semnal" },
          "content": {
            "en": "The term **signal corruption** (overriding) is used when the programmer sets a custom handler for a signal type. Configuring a specific **signal handling behavior** from the three options above can be done by calling the `signal` or `sigaction` primitives.",
            "ro": "Termenul de **corupere a unui semnal** se foloseste atunci cand programatorul isi seteaza un handler propriu pentru acel tip de semnal. Configurarea unui anumit **comportament de tratare a semnalelor** dintre cele trei de mai sus se poate face apeland una dintre primitivele `signal` sau `sigaction`."
          }
        },
        {
          "type": "callout",
          "variant": "warning",
          "content": {
            "en": "**Important:** The signals `SIGKILL` and `SIGSTOP` cannot be corrupted, ignored, or blocked! These two signals always execute their default behavior (terminating and stopping the process, respectively), regardless of any handler configuration.",
            "ro": "**Important:** Semnalele `SIGKILL` si `SIGSTOP` nu pot fi corupte, ignorate sau blocate! Aceste doua semnale executa intotdeauna comportamentul lor implicit (terminarea, respectiv oprirea procesului), indiferent de orice configurare de handler."
          }
        },
        {
          "type": "quiz",
          "questions": [
            {
              "question": { "en": "Which of the following signals CANNOT be caught or ignored?", "ro": "Care dintre urmatoarele semnale NU poate fi captat sau ignorat?" },
              "options": [
                { "text": { "en": "SIGINT", "ro": "SIGINT" }, "correct": false },
                { "text": { "en": "SIGTERM", "ro": "SIGTERM" }, "correct": false },
                { "text": { "en": "SIGKILL", "ro": "SIGKILL" }, "correct": true },
                { "text": { "en": "SIGUSR1", "ro": "SIGUSR1" }, "correct": false }
              ],
              "explanation": { "en": "`SIGKILL` (and `SIGSTOP`) cannot be caught, ignored, or blocked. They always execute their default action. `SIGINT`, `SIGTERM`, and `SIGUSR1` can all be caught with a custom handler or ignored.", "ro": "`SIGKILL` (si `SIGSTOP`) nu pot fi captate, ignorate sau blocate. Ele executa intotdeauna actiunea lor implicita. `SIGINT`, `SIGTERM` si `SIGUSR1` pot fi captate cu un handler personalizat sau ignorate." }
            }
          ]
        }
      ]
    },
    {
      "id": "os-c10-signal-primitive",
      "title": { "en": "The signal Primitive", "ro": "Primitiva signal" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "The `signal` function is used to configure how a process handles a specific signal type. The parameter `signum` is the signal type to configure, and `handler` is the action (i.e., the signal handler) to associate with that signal. The `handler` can be: a pointer to a user-defined function, `SIG_DFL` (restore the default handler), or `SIG_IGN` (ignore the signal). The function returns the old handler for the specified signal, or `SIG_ERR` on error.",
            "ro": "Functia `signal` este folosita pentru a configura modul in care un proces trateaza un anumit tip de semnal. Parametrul `signum` este tipul semnalului de configurat, iar `handler` este actiunea (i.e., handler-ul de semnal) ce se asociaza acelui semnal. `handler` poate fi: un pointer la o functie definita de programator, `SIG_DFL` (restaureaza handler-ul implicit), sau `SIG_IGN` (ignora semnalul). Functia returneaza vechiul handler pentru semnalul specificat, sau `SIG_ERR` in caz de eroare."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "#include <signal.h>\n\n// Interface of the signal function:\nsighandler_t signal(int signum, sighandler_t handler);\n\n// signum  = signal type to configure\n// handler = SIG_DFL | SIG_IGN | pointer to user function\n// Returns: previous handler, or SIG_ERR on error\n\n// The handler function type:\ntypedef void (*sighandler_t)(int);\n// i.e., a function returning void, with an int argument"
        },
        {
          "type": "learn",
          "content": {
            "en": "The effect of `signal` is: for the specified signal type, the specified handler is associated. Subsequently (until a new reconfiguration), every time the process receives signal `signum`, the handler of signal `signum` will execute. If the `handler` argument is a user-defined function, that function must have the prototype `void handler_name(int)` -- at the time of handler execution, the argument will have the value of the signal number that caused the handler to execute.",
            "ro": "Efectul functiei `signal` este: acelui tip de semnal i se asociaza handler-ul specificat. Ca urmare, ulterior (pana la o noua reconfigurare), ori de cate ori procesul va receptiona semnalul `signum`, se va executa handler-ul de semnal `signum`. Daca argumentul `handler` este o functie definita de programator, aceasta functie trebuie sa aiba prototipul `void handler_name(int)` -- la momentul executiei handler-ului, argumentul va avea valoarea numarului semnalului ce a determinat executia acelui handler."
          }
        },
        {
          "type": "callout",
          "variant": "info",
          "content": {
            "en": "A handler can be assigned to the same function for multiple signal types. At the time of handler execution, the argument receives the value of the signal number that triggered it, allowing the handler to distinguish which signal caused the call. It is generally **not recommended** to ignore signals, especially those representing important events. If you want the program not to receive signals during a critical section, the recommended solution is to **block** signal reception, not to ignore them.",
            "ro": "Se poate asigna o aceeasi functie ca handler pentru mai multe semnale. La momentul executiei handler-ului, argumentul primeste valoarea numarului semnalului ce a determinat executia, permitand handler-ului sa distinga care semnal a cauzat apelul. In general **nu se recomanda** ca programul sa ignore semnalele, mai ales pe cele care reprezinta evenimente importante. Daca se doreste ca programul sa nu receptioneze semnale in timpul executiei unei portiuni critice de cod, solutia recomandata este sa se **blocheze** primirea semnalelor, nu ca ele sa fie ignorate."
          }
        }
      ]
    },
    {
      "id": "os-c10-code-examples",
      "title": { "en": "Demo: Code Examples", "ro": "Demo: exemple de cod" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "Example: a program that ignores keyboard interrupts, i.e., the `SIGINT` signal (generated by `Ctrl+C`) and the `SIGQUIT` signal (generated by `Ctrl+\\`). The program `sig_ex1.c` runs without ignoring these two signals (can be interrupted/stopped with `Ctrl+C` or `Ctrl+\\`), while `sig_ex2.c` ignores them (runs without being stoppable via keyboard).",
            "ro": "Exemplu: un program care ignora intreruperile de tastatura, adica semnalul `SIGINT` (generat de `Ctrl+C`) si semnalul `SIGQUIT` (generat de `Ctrl+\\`). Programul `sig_ex1.c` ruleaza fara ignorarea celor doua semnale (poate fi intrerupt/oprit cu `Ctrl+C` sau `Ctrl+\\`), in timp ce `sig_ex2.c` le ignoreaza (ruleaza fara a putea fi oprit de la tastatura)."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "// sig_ex2.c - Ignoring SIGINT and SIGQUIT\n#include <stdio.h>\n#include <signal.h>\n#include <unistd.h>\n\nint main() {\n    // Ignore SIGINT (Ctrl+C) and SIGQUIT (Ctrl+\\)\n    signal(SIGINT, SIG_IGN);\n    signal(SIGQUIT, SIG_IGN);\n\n    printf(\"Try pressing Ctrl+C or Ctrl+\\\\ ...\\n\");\n    while (1) {\n        printf(\"Running...\\n\");\n        sleep(1);\n    }\n    return 0;\n}"
        },
        {
          "type": "learn",
          "content": {
            "en": "We can modify the example to override signals with a custom handler that displays a message, and then restore the default signal behavior. The program `sig_ex3.c` demonstrates using a custom handler function.",
            "ro": "Putem modifica exemplul pentru a corupe semnalele sa execute un handler propriu care afiseaza un anumit mesaj, si apoi refacem comportamentul implicit al semnalelor. Programul `sig_ex3.c` demonstreaza folosirea unei functii handler personalizate."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "// sig_ex3.c - Custom signal handler + restoring default\n#include <stdio.h>\n#include <signal.h>\n#include <unistd.h>\n\nvoid my_handler(int signum) {\n    printf(\"\\nCaught signal %d\\n\", signum);\n    // Restore default handler so next signal terminates\n    signal(signum, SIG_DFL);\n}\n\nint main() {\n    // Set custom handler for SIGINT and SIGQUIT\n    signal(SIGINT, my_handler);\n    signal(SIGQUIT, my_handler);\n\n    printf(\"First Ctrl+C prints message, second terminates.\\n\");\n    while (1) {\n        printf(\"Running...\\n\");\n        sleep(1);\n    }\n    return 0;\n}"
        },
        {
          "type": "think",
          "question": {
            "en": "In sig_ex3.c, what happens if the user presses Ctrl+C twice? Trace through the signal handling.",
            "ro": "In sig_ex3.c, ce se intampla daca utilizatorul apasa Ctrl+C de doua ori? Urmariti tratarea semnalului."
          },
          "answer": {
            "en": "First Ctrl+C: SIGINT is delivered, `my_handler` executes, prints the message, then restores the default handler via `signal(SIGINT, SIG_DFL)`. The process continues running. Second Ctrl+C: SIGINT is delivered again, but now the default handler is active, so the process is terminated. This is a common pattern: handle the signal once for cleanup, then restore default behavior.",
            "ro": "Primul Ctrl+C: SIGINT este livrat, `my_handler` se executa, afiseaza mesajul, apoi restaureaza handler-ul implicit prin `signal(SIGINT, SIG_DFL)`. Procesul continua sa ruleze. Al doilea Ctrl+C: SIGINT este livrat din nou, dar acum handler-ul implicit este activ, deci procesul este terminat. Acesta este un pattern comun: tratezi semnalul o data pentru cleanup, apoi restaurezi comportamentul implicit."
          }
        }
      ]
    },
    {
      "id": "os-c10-custom-handlers",
      "title": { "en": "Defining Custom Signal Handlers", "ro": "Definirea propriilor handlere de semnal" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "A custom signal handler is a function defined by the programmer, which will be called when the process receives the signal to which it is associated. The main strategies used when writing custom handlers are: (1) The handler **notifies** signal reception by setting a global variable and then returns immediately; the main loop of the program periodically checks if that variable has been set, and if so, performs the desired operations. (2) The handler can **terminate** the process execution, or transfer execution to a point from which the process can recover the state it was in at the time of signal reception.",
            "ro": "Un handler de semnal propriu este o functie definita de programator, care va fi apelata atunci cand procesul receptioneaza semnalul caruia ii este asociata. Strategiile principale folosite in scrierea de handlere proprii sunt: (1) Handler-ul **notifica** primirea semnalului prin setarea unei variabile globale si apoi se returneaza imediat; in bucla principala a programului se verifica periodic daca acea variabila a fost setata, si in caz ca da, se vor efectua operatiile dorite. (2) Handler-ul poate sa **termine** executia procesului, sau sa transfere executia intr-un punct in care procesul poate sa-si recupereze starea in care se afla in momentul receptionarii semnalului."
          }
        },
        {
          "type": "callout",
          "variant": "warning",
          "content": {
            "en": "**Caution:** Special measures must be taken when writing signal handler code, because handlers can be called asynchronously during program execution, at unpredictable times. For example, while a handler associated with a received signal is executing, it can be interrupted by the reception of another signal (the second signal must be of a different type; if it is the same type, it will be blocked until the first handler completes). Therefore, receiving a signal can interrupt not only the program execution, but even the execution of a handler for a previously received signal, or a system call being executed at that moment.",
            "ro": "**Atentie:** Trebuie luate masuri speciale atunci cand se scrie codul pentru handler-ele de semnal, deoarece acestea pot fi apelate asincron pe parcursul executiei programului, la momente de timp imprevizibile. Spre exemplu, in timp ce se executa handler-ul asociat unui semnal primit, acesta poate fi intrerupt prin receptia unui alt semnal (al doilea semnal trebuie sa fie de alt tip; daca este acelasi tip de semnal, va fi blocat pana la terminarea primului handler). Prin urmare, primirea unui semnal poate intrerupe nu doar executia programului respectiv, ci chiar executia handler-ului unui semnal anterior primit, sau executia unui apel de sistem efectuat in acel moment."
          }
        },
        {
          "type": "think",
          "question": {
            "en": "Why is it safer to use a global volatile flag in a signal handler instead of performing complex operations directly inside the handler?",
            "ro": "De ce este mai sigur sa folosim un flag global volatile intr-un handler de semnal in loc sa efectuam operatii complexe direct in handler?"
          },
          "answer": {
            "en": "Signal handlers execute asynchronously and can interrupt the program at any point, including in the middle of non-reentrant functions (like `printf`, `malloc`). If the handler calls the same function, it can corrupt internal data structures, causing crashes or undefined behavior. By only setting a `volatile sig_atomic_t` flag in the handler and checking it in the main loop, we avoid calling non-reentrant functions from signal context. The `volatile` keyword ensures the compiler does not optimize away reads of the flag variable.",
            "ro": "Handler-ele de semnal se executa asincron si pot intrerupe programul in orice punct, inclusiv in mijlocul unor functii non-reentrante (cum ar fi `printf`, `malloc`). Daca handler-ul apeleaza aceeasi functie, poate corupe structurile de date interne, cauzand crash-uri sau comportament nedefinit. Setand doar un flag `volatile sig_atomic_t` in handler si verificandu-l in bucla principala, evitam apelarea functiilor non-reentrante din contextul semnalului. Cuvantul cheie `volatile` asigura ca compilatorul nu optimizeaza citirile variabilei flag."
          }
        }
      ]
    },
    {
      "id": "os-c10-signal-blocking",
      "title": { "en": "Signal Blocking", "ro": "Blocarea semnalelor" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "**Signal blocking** means that the process tells the operating system not to deliver certain signals (they will remain in the signal queue until the process unblocks their reception). It is **not recommended** for a program to block signals for its entire execution, but only during critical code sections. If a signal arrives during the execution of a critical section, it will be delivered after the section completes and the signal type is unblocked.",
            "ro": "**Blocarea semnalelor** inseamna ca procesul spune sistemului de operare sa nu ii transmita anumite semnale (ele vor ramane in coada de semnale pana cand procesul va debloca primirea lor). **Nu este recomandat** ca un program sa blocheze semnalele pe tot parcursul executiei sale, ci numai pe durata executiei unor portiuni critice ale codului sau. Daca un semnal ajunge in timpul executiei unei portiuni critice, el va fi livrat procesului abia dupa terminarea executiei acelei sectiuni si deblocarea acelui tip de semnal."
          }
        },
        {
          "type": "learn",
          "content": {
            "en": "Signal blocking is done with the `sigprocmask` function, which uses the `sigset_t` data structure (a bitmask representing the set of signals selected for blocking). The `sigpending` primitive can be used to check the existence of pending (queued) blocked signals in the signal queue (i.e., signals currently waiting to be unblocked so they can be delivered to the process).",
            "ro": "Blocarea semnalelor se realizeaza cu functia `sigprocmask`, ce utilizeaza structura de date `sigset_t` (care este o masca de biti, cu semnificatia de set de semnal ales pentru blocare). Cu primitiva `sigpending` se poate verifica existenta in coada de semnale a unor semnale blocate (i.e., semnale care asteapta sa fie deblocate pentru a putea fi livrate procesului)."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "#include <signal.h>\n\n// Block/unblock signals using a signal set\nint sigprocmask(int how, const sigset_t *set, sigset_t *oldset);\n// how: SIG_BLOCK, SIG_UNBLOCK, or SIG_SETMASK\n\n// Check for pending (blocked) signals\nint sigpending(sigset_t *set);\n\n// Signal set manipulation functions:\nint sigemptyset(sigset_t *set);    // clear all signals\nint sigfillset(sigset_t *set);     // add all signals\nint sigaddset(sigset_t *set, int signum);  // add one signal\nint sigdelset(sigset_t *set, int signum);  // remove one signal\nint sigismember(const sigset_t *set, int signum); // test"
        },
        {
          "type": "code",
          "language": "c",
          "code": "// sig_ex4.c - Blocking SIGINT during a critical section\n#include <stdio.h>\n#include <signal.h>\n#include <unistd.h>\n\nint main() {\n    sigset_t block_set, old_set;\n\n    sigemptyset(&block_set);\n    sigaddset(&block_set, SIGINT);\n\n    // Block SIGINT\n    sigprocmask(SIG_BLOCK, &block_set, &old_set);\n\n    printf(\"SIGINT blocked for 5 seconds...\\n\");\n    sleep(5);  // Critical section\n\n    // Unblock SIGINT (pending signals delivered now)\n    sigprocmask(SIG_SETMASK, &old_set, NULL);\n    printf(\"SIGINT unblocked.\\n\");\n\n    sleep(5);\n    return 0;\n}"
        },
        {
          "type": "quiz",
          "questions": [
            {
              "question": { "en": "What happens to a blocked signal when it is generated?", "ro": "Ce se intampla cu un semnal blocat cand este generat?" },
              "options": [
                { "text": { "en": "It is discarded (lost forever)", "ro": "Este pierdut (pierdut definitiv)" }, "correct": false },
                { "text": { "en": "It remains in the signal queue until unblocked", "ro": "Ramane in coada de semnale pana la deblocare" }, "correct": true },
                { "text": { "en": "It is delivered immediately, bypassing the block", "ro": "Este livrat imediat, ocolind blocarea" }, "correct": false },
                { "text": { "en": "It causes an error in the sending process", "ro": "Cauzeaza o eroare in procesul care trimite" }, "correct": false }
              ],
              "explanation": { "en": "Blocked signals are not discarded -- they remain pending in the signal queue. When the process unblocks that signal type (via `sigprocmask`), all pending signals of that type are delivered.", "ro": "Semnalele blocate nu sunt pierdute -- raman in asteptare in coada de semnale. Cand procesul deblocheaza acel tip de semnal (prin `sigprocmask`), toate semnalele in asteptare de acel tip sunt livrate." }
            }
          ]
        }
      ]
    },
    {
      "id": "os-c10-waiting-for-signal",
      "title": { "en": "Waiting for a Signal", "ro": "Asteptarea unui semnal" },
      "blocks": [
        {
          "type": "learn",
          "content": {
            "en": "If the application is influenced by external events, or uses signals for synchronization with other processes, then it may need to do nothing else but wait for signal reception. The `pause` function can be used for this purpose. It suspends program execution until a signal arrives. However, `pause()` has a simplicity that can hide hard-to-detect errors: since the main program does nothing else but call `pause()`, it means that most of the useful activity in the program is realized by the signal handlers.",
            "ro": "Daca aplicatia este influentata de evenimente externe, sau foloseste semnale pentru sincronizare cu alte procese, atunci ea nu trebuie sa faca altceva decat sa astepte primirea de semnale. Se poate folosi functia `pause` in acest scop. Ea suspenda executia programului pana la sosirea unui semnal. Totusi, `pause()` are o simplitate care poate ascunde erori greu de detectat: deoarece programul principal nu face altceva decat sa apeleze `pause()`, inseamna ca cea mai mare parte a activitatii utile in program o realizeaza handler-ele de semnal."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "#include <unistd.h>\n\nint pause(void);\n// Effect: suspends execution until a signal arrives\n// Always returns -1 with errno set to EINTR"
        },
        {
          "type": "learn",
          "content": {
            "en": "The recommended approach for waiting for a specific signal (i.e., the first received signal from a fixed set of signals) is to use the `sigsuspend` function. It atomically replaces the current signal mask of the process with the mask specified by the `set` parameter, then suspends execution of the process until the reception of a signal that is not in the current (now replaced) signal mask. The signal mask remains the current value (i.e., the value of `set`) until the `sigsuspend` function returns, at which point the mask is automatically restored to its previous value.",
            "ro": "Modalitatea cea mai indicata pentru asteptarea unui anumit semnal (i.e., asteptarea primului semnal primit, dintr-o multime fixata de semnale) este de a folosi functia `sigsuspend`. Aceasta inlocuieste atomic masca de semnale curenta a procesului cu cea specificata de parametrul `set`, apoi suspenda executia procesului pana la receptionarea unui semnal care nu este in masca de semnale curenta (cea inlocuita). Masca de semnale ramane valoarea curenta (i.e., valoarea lui `set`) pana cand functia `sigsuspend` returneaza, moment in care masca este restaurata automat la valoarea anterioara."
          }
        },
        {
          "type": "code",
          "language": "c",
          "code": "#include <signal.h>\n\nint sigsuspend(const sigset_t *set);\n// Effect: atomically replaces signal mask with *set,\n//         then suspends until a signal not in *set arrives\n// Returns: always -1 (errno = EINVAL, EFAULT, or EINTR)"
        },
        {
          "type": "code",
          "language": "c",
          "code": "// Example: suspend execution waiting for SIGQUIT\n#include <stdio.h>\n#include <signal.h>\n\nvoid handler(int sig) {\n    printf(\"Received SIGQUIT\\n\");\n}\n\nint main() {\n    sigset_t mask;\n\n    signal(SIGQUIT, handler);\n\n    // Block all signals except SIGQUIT\n    sigfillset(&mask);\n    sigdelset(&mask, SIGQUIT);\n\n    printf(\"Waiting for SIGQUIT (Ctrl+\\\\)...\\n\");\n    sigsuspend(&mask);  // Atomically unblock SIGQUIT and wait\n\n    printf(\"Resumed after signal.\\n\");\n    return 0;\n}"
        },
        {
          "type": "callout",
          "variant": "tip",
          "content": {
            "en": "Prefer `sigsuspend` over `pause` when waiting for specific signals. The `sigsuspend` function is atomic (it replaces the mask and suspends in one operation), avoiding race conditions where a signal could arrive between unblocking and the actual wait. With `pause`, the signal to be waited for might arrive before `pause` is called, causing the program to block indefinitely.",
            "ro": "Preferati `sigsuspend` in locul `pause` cand asteptati semnale specifice. Functia `sigsuspend` este atomica (inlocuieste masca si suspenda intr-o singura operatie), evitand conditii de cursa in care un semnal ar putea sosi intre deblocare si asteptarea efectiva. Cu `pause`, semnalul asteptat ar putea sosi inainte ca `pause` sa fie apelat, cauzand blocarea indefinita a programului."
          }
        },
        {
          "type": "quiz",
          "questions": [
            {
              "question": { "en": "Why is sigsuspend preferred over pause for waiting for a specific signal?", "ro": "De ce este sigsuspend preferat fata de pause pentru asteptarea unui semnal specific?" },
              "options": [
                { "text": { "en": "sigsuspend is faster than pause", "ro": "sigsuspend este mai rapid decat pause" }, "correct": false },
                { "text": { "en": "sigsuspend is atomic, avoiding race conditions between unmasking and waiting", "ro": "sigsuspend este atomic, evitand conditii de cursa intre demascare si asteptare" }, "correct": true },
                { "text": { "en": "pause cannot be interrupted by any signal", "ro": "pause nu poate fi intrerupt de niciun semnal" }, "correct": false },
                { "text": { "en": "sigsuspend works with real-time signals, while pause does not", "ro": "sigsuspend functioneaza cu semnale real-time, in timp ce pause nu" }, "correct": false }
              ],
              "explanation": { "en": "`sigsuspend` atomically replaces the signal mask and suspends execution in a single system call. This prevents the race condition where a signal could arrive between calling `sigprocmask` to unblock and calling `pause` to wait. With separate calls, the signal might be delivered before `pause` executes, causing a deadlock.", "ro": "`sigsuspend` inlocuieste atomic masca de semnale si suspenda executia intr-un singur apel de sistem. Aceasta previne conditia de cursa in care un semnal ar putea fi livrat intre apelul `sigprocmask` pentru deblocare si apelul `pause` pentru asteptare. Cu apeluri separate, semnalul ar putea fi livrat inainte ca `pause` sa se execute, cauzand un deadlock." }
            }
          ]
        }
      ]
    }
  ]
}